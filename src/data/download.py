"""Member 1, W1.2 — fetch 3W from Figshare/GitHub, verify checksums."""


def download(target_dir: str) -> None:
    # See data/download_data.sh for the interim git-clone approach.
    raise NotImplementedError
